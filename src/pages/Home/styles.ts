import styled from "styled-components";

export const BackgroundBlur = styled.div`
  width: 100vw;
  height: 100vh;
  position: absolute;
  max-width: 100%;
  top: 0;
  left: 0;
  filter: blur(10rem);
`;
